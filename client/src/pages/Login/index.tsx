import { Box } from '@chakra-ui/react';
import LoginForm from '../../components/Login Form';

const LoginPage = () => {
	return (
		<Box
			p={'1rem'}
			m={'0 auto'}>
			<LoginForm />
		</Box>
	);
};

export default LoginPage;
